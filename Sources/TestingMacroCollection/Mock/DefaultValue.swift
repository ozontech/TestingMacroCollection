/// Mode for generating default values for non-optional properties.
///
public enum DefaultValue {
    /// Generation of static default values.
    case `static`
    /// No generation.
    case none
}
